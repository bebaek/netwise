# Generate this value with: openssl rand -hex 32
NETWISE_POSTGRES_PASSWORD=

# Bind to 127.0.0.1 when an HTTPS reverse proxy runs on the same host.
NETWISE_HTTP_BIND_ADDRESS=127.0.0.1
NETWISE_HTTP_PORT=8080
